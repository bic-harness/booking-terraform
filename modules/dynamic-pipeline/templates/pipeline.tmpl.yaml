harnessApiVersion: '1.0'
type: PIPELINE
pipelineStages:
- type: ENV_STATE
  name: ${VersionsWorkflowName}
  parallel: false
  skipCondition:
    type: DO_NOT_SKIP
  stageName: Set Versions
  workflowName: ${VersionsWorkflowName}
${stages}
